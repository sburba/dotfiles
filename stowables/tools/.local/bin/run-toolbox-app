#/usr/bin/env bash
set -euo pipefail

app_name=$1
lowercase_app_name="${app_name,,}"

exe=$(find ~/.local/share/JetBrains/Toolbox/apps/${app_name} -name "${lowercase_app_name}.sh" | sort -r | head -n1)

eval $exe
